import { defineConfig } from 'vite';

// If your GitHub Pages repo is NOT named "portfolio", change `base` below
// to match it exactly, e.g. base: '/My-Repo-Name/'.
// If you're deploying to a custom domain or a username.github.io root repo,
// set base: '/' instead.
export default defineConfig({
  base: '/Portfolio/',
});
